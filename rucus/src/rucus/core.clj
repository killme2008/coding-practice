(ns rucus.core)

(defn read
  "Read a word"
  []
  )

(defn build-table []
  "Build table[focus, word] = p")

(defn read-bnf []
  "Read bnf to tokens")

(defn first-set [ts s]
  "calcuate FIRST(s)")

(defn follow-set [ts s]
  "calcuate follow(s)")

(defn first+-set [ts s]
  "calcuate first+(s)")

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))
